`cd install/openssl-1.1.1h && ./install.sh` will download OpenSSL sources,
build deb package and install it.


# Remove local package from apt sources
```
cd deb
./rem.sh <filename>.deb
```


# Requirements
* aptitude
* checkinstall
* sudo
